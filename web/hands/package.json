{
  "name": "@intelstream/hands-activity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "build": "tsc --noEmit && vite build && npm run scan:build",
    "scan:build": "node scripts/scan-build.mjs",
    "generate:fighter": "esbuild scripts/generate-fighter-glb.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/generate-fighter-glb.mjs && node node_modules/.cache/generate-fighter-glb.mjs",
    "generate:audio": "uv run --project ../.. --with numpy==2.5.1 python scripts/generate-injury-sounds.py"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "2.5.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "@types/three": "0.185.4",
    "esbuild": "0.28.1",
    "jsdom": "29.1.1",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
